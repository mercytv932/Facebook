This project is a simple JavaScript login simulation that checks a username and password against a small in-memory database.
If the login is successful, a mock news feed is displayed in the browser console. If not, the user gets an error message.
